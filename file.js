// @ Socket.IO is a JavaScript library for real-time web applications. 
// @ It enables real-time, bi-directional communication between web clients and servers. 
// @ Socket.IO can be used with both Node.js on the server side and with client-side JavaScript in the browser.
// @ It allows developers to create applications that can send and receive real-time data, such as chat applications, 
//   online games, and collaborative editing tools.

//npm install -g express
   //This will install Express globally on your system, which means it will be available to use in any Node.js project without needing to install it separately for each project.

// npm install express
   //This will install Express as a local dependency for the project, which means it will only be available for that specific project.





// const http = require('http').createServer(app); 
// - Create an HTTP server instance using the Express app.
// const io = require('socket.io')(http); 
// - Require and initialize the Socket.IO library, passing the HTTP server instance as an argument.
//  create srver is the method with in http module  and  is used to create an HTTP server instance (The server is responsible for receiving and processing incoming HTTP requests from clients,)
//  Essentially, the createServer method allows you to create a server that can listen for incoming HTTP requests and handle them appropriately based on your application's logic.
  
  
// io.on('connection', (socket) => { ... }); 
// - Listen for incoming connections from clients and handle them with a callback function.

//first install dependency
